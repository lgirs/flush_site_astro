---
export const pageTitle = "Style Guide";
export const pageTags = [];

import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Style Guide" description="Visual reference for Flush site styles">
  <meta name="robots" content="noindex,nofollow" />

  <section class="sg">
    <h1>Style Guide</h1>
    <p class="muted">Internal reference for fonts, colors, components.</p>

    <h2>Colors</h2>
    <div class="swatches">
      <div class="swatch" style="background:#0f0d0d;color:#fff"><span>#0F0D0D</span><small>Black (bg)</small></div>
      <div class="swatch" style="background:#e9e8f1;color:#000"><span>#E9E8F1</span><small>Light (text)</small></div>
      <div class="swatch" style="background:#f5d547;color:#000"><span>#F5D547</span><small>Yellow (highlight)</small></div>
      <div class="swatch" style="background:#962800;color:#fff"><span>#962800</span><small>Legacy red</small></div>
      <div class="swatch" style="background:#ffffff;color:#000"><span>#FFFFFF</span><small>White</small></div>
    </div>

    <h2>Typography — Oswald (global)</h2>
    <h1>H1 — Big Heading</h1>
    <h2>H2 — Section Heading</h2>
    <h3>H3 — Subsection</h3>
    <p>Default body text (Oswald 400). We believe music is a community thing and we are better off together.</p>
    <p><strong>Bold</strong> · <em>Italic</em> · <code>Inline code</code></p>

    <h2>Archivo long body (via .long-text)</h2>
    <div class="long-text card">
      <p>This block shows Archivo, used for long-form content. Wrap content in a container with class <code>.long-text</code>.</p>
      <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit.</p>
    </div>

    <h2>Links</h2>
    <p>Default link: <a href="#">Hover me</a></p>
    <a class="button" href="#">Example Button</a>

    <h2>Tag chips preview</h2>
    <div class="cloud">
      <a href="#" class="cloud-tag cloud-tag--xl-left">gigs</a>
      <a href="#" class="cloud-tag cloud-tag--md-right">lyrics</a>
      <a href="#" class="cloud-tag cloud-tag--lg-left">video</a>
      <a href="#" class="cloud-tag cloud-tag--sm-right">music</a>
    </div>
  </section>
</BaseLayout>

<style>
  .sg { display:grid; gap:1.5rem; }
  .muted { opacity:.8; }

  .swatches { display:grid; grid-template-columns: repeat(auto-fit, minmax(180px,1fr)); gap:.75rem; }
  .swatch { border:1px solid rgba(255,255,255,.08); padding:.75rem; border-radius:.5rem; display:flex; flex-direction:column; }
  .swatch span { font-weight:700; }

  .card { border:1px solid rgba(233,232,241,.15); border-radius:.5rem; padding:1rem; }

  .cloud { display:flex; flex-wrap:wrap; gap:.4rem .7rem; align-items:center; }
  .cloud-tag {
    display:inline-block;
    font-family:"Oswald", Impact, Haettenschweiler, "Arial Narrow Bold", sans-serif;
    line-height:1;
    color:var(--flush-light);
    letter-spacing:.5px;
    text-transform:uppercase;
    text-decoration:none;
    transition: transform .12s ease, color .12s ease, text-shadow .12s ease;
  }
  .cloud-tag:hover { color:var(--flush-yellow); text-shadow:0 2px 0 rgba(0,0,0,.35); }

  .cloud-tag--xl-left  { transform: rotate(-4deg) scale(1.4); font-weight:700; }
  .cloud-tag--lg-left  { transform: rotate(-3deg) scale(1.2); font-weight:700; }
  .cloud-tag--md-right { transform: rotate(3deg)  scale(1.05); font-weight:600; }
  .cloud-tag--sm-right { transform: rotate(2deg)  scale(0.95); font-weight:600; }

  .button { display:inline-block; padding:.5rem 1rem; border:1px solid var(--flush-yellow); border-radius:999px; color:var(--flush-yellow); }
  .button:hover { background:var(--flush-yellow); color:#000; }
</style>
