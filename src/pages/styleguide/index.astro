---
export const pageTitle = "Style Guide";
export const pageTags = [];

import BaseLayout from "../../layouts/BaseLayout.astro";
---

<meta name="robots" content="noindex,nofollow" />

<BaseLayout title="Style Guide" description="Visual reference for Flush site styles">
  <section class="sg">
    <h1>Style Guide</h1>
    <p class="muted">Internal reference for fonts, colors, components, and Markdown/MDX patterns.</p>

    <!-- Colors -->
    <h2>Colors</h2>
    <div class="swatches">
      <div class="swatch" style="--c: var(--flush-black, #0f0d0d)"><span>#0F0D0D</span><small>Black (bg)</small></div>
      <div class="swatch" style="--c: var(--flush-light, #e9e8f1)"><span>#E9E8F1</span><small>Light (text)</small></div>
      <div class="swatch" style="--c: var(--flush-yellow, #f5d547)"><span>#F5D547</span><small>Yellow (highlight)</small></div>
      <div class="swatch" style="--c: var(--flush-red, #962800)"><span>#962800</span><small>Legacy red</small></div>
      <div class="swatch" style="--c: #ffffff"><span>#FFFFFF</span><small>Pure white</small></div>
    </div>

    <!-- Fonts -->
    <h2>Typography — Oswald (global)</h2>
    <h1>H1 — Big Heading</h1>
    <h2>H2 — Section Heading</h2>
    <h3>H3 — Subsection</h3>
    <p>Default body text (Oswald 400). We believe music is a community thing and we are better off together.</p>
    <p><strong>Bold</strong> · <em>Italic</em> · <code>Inline code</code></p>

    <h2>Long body — Archivo via .long-text</h2>
    <div class="long-text card">
      <p>This block shows Archivo, used for long-form content. Wrap in <code>.long-text</code> to apply.</p>
      <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nullam fringilla neque a orci finibus, non egestas nulla posuere.</p>
    </div>

    <!-- Links / buttons -->
    <h2>Links</h2>
    <p>Default link: <a href="#">Hover me</a></p>
    <a class="button" href="#">Example Button</a>

    <!-- Tag chips -->
    <h2>Tag chips</h2>
    <div class="cloud">
      <a href="#" class="cloud-tag" style="--scale:1.2;--rot:-3deg;--w:700;">gigs</a>
      <a href="#" class="cloud-tag" style="--scale:1.0;--rot:2deg;--w:600;">lyrics</a>
      <a href="#" class="cloud-tag" style="--scale:1.4;--rot:-6deg;--w:700;">video</a>
      <a href="#" class="cloud-tag" style="--scale:0.95;--rot:4deg;--w:600;">music</a>
    </div>

    <!-- Markdown patterns -->
    <h2>Markdown patterns</h2>
    <div class="card long-text">
      <h3>Headings</h3>
      <pre><code>## H2 heading
### H3 heading</code></pre>

      <h3>Paragraphs & emphasis</h3>
      <p>Lorem ipsum dolor sit amet. <strong>Bold text</strong>, <em>italic text</em>, <code>inline code</code>.</p>

      <h3>Blockquote</h3>
      <blockquote>
        “This is a blockquote. Use &gt; at the start of a line in Markdown.”
      </blockquote>

      <h3>Lists</h3>
      <p>Unordered:</p>
      <ul>
        <li>First item</li>
        <li>Second item</li>
      </ul>
      <p>Ordered:</p>
      <ol>
        <li>Step one</li>
        <li>Step two</li>
      </ol>

      <h3>Code block</h3>
      <pre><code>function hello() {
  console.log("Hello, world!");
}</code></pre>

      <h3>Table</h3>
      <table>
        <thead>
          <tr><th>Album</th><th>Year</th><th>Notes</th></tr>
        </thead>
        <tbody>
          <tr><td>Noises</td><td>2024</td><td>Latest album</td></tr>
          <tr><td>It Began as a Mistake</td><td>2020</td><td>Debut</td></tr>
        </tbody>
      </table>

      <h3>Image</h3>
      <p><img src="/images/logo-flush-white.png" alt="Flush logo" style="max-width:200px" /></p>
    </div>
  </section>
</BaseLayout>

<style>
  .sg { display:grid; gap:1.5rem; }
  .muted { opacity:.8; }

  /* Swatches */
  .swatches { display:grid; grid-template-columns: repeat(auto-fit, minmax(180px,1fr)); gap:.75rem; }
  .swatch { background:var(--c); border:1px solid rgba(255,255,255,.08); padding:.75rem; border-radius:.5rem; display:flex; flex-direction:column; }
  .swatch span { font-weight:700; }

  /* Cards */
  .card { border:1px solid rgba(233,232,241,.15); border-radius:.5rem; padding:1rem; }

  /* Tag cloud preview */
  .cloud { display:flex; flex-wrap:wrap; gap:.4rem .7rem; }
  .cloud-tag {
    display:inline-block;
    transform: rotate(var(--rot)) scale(var(--scale));
    font-family:"Oswald",sans-serif;
    font-weight:var(--w,600);
    color:var(--flush-light);
    text-transform:uppercase;
    text-decoration:none;
  }
  .cloud-tag:hover { color:var(--flush-yellow); }

  /* Button */
  .button { display:inline-block; padding:.5rem 1rem; border:1px solid var(--flush-yellow); border-radius:999px; color:var(--flush-yellow); }
  .button:hover { background:var(--flush-yellow); color:#000; }

  /* Tables */
  table { border-collapse:collapse; margin-top:.5rem; }
  th, td { border:1px solid rgba(233,232,241,.3); padding:.4rem .6rem; }
  th { background:rgba(233,232,241,.1); text-align:left; }
</style>
