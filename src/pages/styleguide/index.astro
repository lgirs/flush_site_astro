---
export const pageTitle = "Style Guide";
export const pageTags = [];

import BaseLayout from "../../layouts/BaseLayout.astro";
---

<BaseLayout title="Style Guide" description="Visual reference for Flush site styles">
  <Fragment slot="head">
    <meta name="robots" content="noindex,nofollow" />
  </Fragment>

  <section class="sg">
    <h1>Style Guide</h1>
    <p class="muted">Internal reference for fonts, colors, components, and Markdown/MDX patterns.</p>

    <!-- Colors -->
    <h2>Colors</h2>
    <div class="swatches">
      <div class="swatch c-black"><span>#0F0D0D</span><small>Black (bg)</small></div>
      <div class="swatch c-light"><span>#E9E8F1</span><small>Light (text)</small></div>
      <div class="swatch c-yellow"><span>#F5D547</span><small>Yellow (highlight)</small></div>
      <div class="swatch c-red"><span>#962800</span><small>Legacy red</small></div>
      <div class="swatch c-white"><span>#FFFFFF</span><small>Pure white</small></div>
    </div>

    <!-- Fonts -->
    <h2>Typography — Oswald (global)</h2>
    <h1>H1 — Big Heading</h1>
    <h2>H2 — Section Heading</h2>
    <h3>H3 — Subsection</h3>
    <p>Default body text (Oswald 400). We believe music is a community thing and we are better off together.</p>
    <p><strong>Bold</strong> · <em>Italic</em> · <code>Inline code</code></p>

    <h2>Long body — Archivo via .long-text</h2>
    <div class="long-text card">
      <p>This block shows Archivo, used for long-form content. Wrap content in a container with class <code>.long-text</code> to apply.</p>
      <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nullam fringilla neque a orci finibus, non egestas nulla posuere.</p>
      <ul>
        <li>Readable paragraphs</li>
        <li>Lists and links <a href="#">like this</a></li>
      </ul>
    </div>

    <!-- Links / buttons -->
    <h2>Links</h2>
    <p>Default link: <a href="#">Hover me</a></p>
    <a class="button" href="#">Example Button</a>

    <!-- Tag chips preview -->
    <h2>Tag chips</h2>
    <div class="cloud">
      <a href="#" class="cloud-tag tag-big rot-left w700">gigs</a>
      <a href="#" class="cloud-tag tag-med rot-right w600">lyrics</a>
      <a href="#" class="cloud-tag tag-xl rot-left w700">video</a>
      <a href="#" class="cloud-tag tag-small rot-right w600">music</a>
    </div>

    <!-- Markdown patterns -->
    <h2>Markdown patterns</h2>
    <div class="card long-text">
      <h3>Headings</h3>
      <pre><code>## H2 heading
### H3 heading</code></pre>

      <h3>Paragraphs & emphasis</h3>
      <p>Lorem ipsum dolor sit amet. <strong>Bold text</strong>, <em>italic text</em>, <code>inline code</code>.</p>

      <h3>Blockquote</h3>
      <blockquote>
        “This is a blockquote. Use &gt; at the start of a line in Markdown.”
      </blockquote>

      <h3>Lists</h3>
      <p>Unordered:</p>
      <ul>
        <li>First item</li>
        <li>Second item</li>
      </ul>
      <p>Ordered:</p>
      <ol>
        <li>Step one</li>
        <li>Step two</li>
      </ol>

      <h3>Code block</h3>
      <pre><code>function hello() {
  console.log("Hello, world!");
}</code></pre>

      <h3>Table</h3>
      <table>
        <thead>
          <tr><th>Album</th><th>Year</th><th>Notes</th></tr>
        </thead>
        <tbody>
          <tr><td>Noises</td><td>2024</td><td>Latest album</td></tr>
          <tr><td>It Began as a Mistake</td><td>2020</td><td>Debut</td></tr>
        </tbody>
      </table>

      <h3>Image</h3>
      <p><img src="/images/logo-flush-white.png" alt="Flush logo" class="img-sample" /></p>
    </div>

    <!-- Hero sample -->
    <h2>Hero snippet</h2>
    <div class="hero-sample card">
      <div class="hero-title">WELCOME FRIEND</div>
      <div class="hero-sub">We are Flush. Pleasure to meet you.</div>
    </div>
  </section>
</BaseLayout>

<style>
  .sg { display:grid; gap:1.5rem; }
  .muted { opacity:.8; }

  /* Swatches */
  .swatches { display:grid; grid-template-columns: repeat(auto-fit, minmax(180px,1fr)); gap:.75rem; }
  .swatch { border:1px solid rgba(255,255,255,.08); padding:.75rem; border-radius:.5rem; display:flex; flex-direction:column; }
  .swatch span { font-weight:700; }
  .c-black { background: var(--flush-black, #0f0d0d); color: #fff; }
  .c-light { background: var(--flush-light, #e9e8f1); color: #000; }
  .c-yellow { background: var(--flush-yellow, #f5d547); color: #000; }
  .c-red { background: #962800; color: #fff; }
  .c-white { background: #ffffff; color: #000; }

  /* Cards */
  .card { border:1px solid rgba(233,232,241,.15); border-radius:.5rem; padding:1rem; }

  /* Tag cloud preview (class-based, no inline style strings) */
  .cloud { display:flex; flex-wrap:wrap; gap:.4rem .7rem; align-items:center; }
  .cloud-tag {
    display:inline-block;
    font-family:"Oswald", Impact, Haettenschweiler, "Arial Narrow Bold", sans-serif;
    line-height:1;
    color:var(--flush-light);
    letter-spacing:.5px;
    text-transform:uppercase;
    text-decoration:none;
    transition: transform .12s ease, color .12s ease, text-shadow .12s ease;
  }
  .cloud-tag:hover { color:var(--flush-yellow); text-shadow:0 2px 0 rgba(0,0,0,.35); }
  .tag-xl { transform: scale(1.4); font-weight:700; }
  .tag-big { transform: scale(1.2); font-weight:700; }
  .tag-med { transform: scale(1.05); font-weight:600; }
  .tag-small { transform: scale(0.95); font-weight:600; }
  .rot-left { transform: rotate(-4deg); }
  .rot-right { transform: rotate(3deg); }
  .w700 { font-weight:700; }
  .w600 { font-weight:600; }

  /* Button */
  .button { display:inline-block; padding:.5rem 1rem; border:1px solid var(--flush-yellow); border-radius:999px; color:var(--flush-yellow); }
  .button:hover { background:var(--flush-yellow); color:#000; }

  /* Tables */
  table { border-collapse:collapse; margin-top:.5rem; width:100%; }
  th, td { border:1px solid rgba(233,232,241,.3); padding:.4rem .6rem; }
  th { background:rgba(233,232,241,.1); text-align:left; }

  /* Images */
  .img-sample { max-width:200px; height:auto; }

  /* Hero sample */
  .hero-sample { background:#1a1616; }
  .hero-title {
    font-family:"Oswald", sans-serif;
    font-weight:700;
    font-size: clamp(28px, 6vw, 56px);
    letter-spacing:.5px;
  }
  .hero-sub {
    font-family:"Oswald", sans-serif;
    font-weight:400;
    color: var(--flush-yellow, #f5d547);
    margin-top:.25rem;
  }
</style>
